#include "jeton.h"

/**
 * @brief Calcul de l'image de x
 *
 * @param L'arbre a, constitué de la fonction mathématique à réaliser
 * @param Variable x
 * @return Retourne le résultat
 *
 * @post Retourne l'image de x
 * @invariant L'arbre n'est pas modifier
 */
float Evaluateur(Arbre a, float x);
